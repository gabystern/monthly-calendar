doctype html
html
  head
  body
  = form_for @event, method: :post do |f|
      = f.label :event_name
      = f.text_field :name
      = f.label :event_time
      = f.datetime_select(:time)
      = f.submit
